# check if necessary variables are correctly defined
LD_MSCL ?= -L${SOLPSLIB} -lmscl

FC       = ftn
ifdef SOLPS_DEBUG
FFLAGS   = -fbounds-check -fcheck=all -fbacktrace
else
FFLAGS   = -O
endif
CPP      = /lib/cpp -traditional

#!!!Local Variables:
#!!! mode: Makefile
#!!! End:

