# check if necessary variables are correctly defined
MSCL_ROOT ?= /pfs/work/g2dpc/SOLPS-ITER/${COMPILER}/mscl_1.2.4
LD_MSCL ?= -L${MSCL_ROOT}/lib -lmscl

ifdef SOLPS_DEBUG
FFLAGS   = -fcheck=all -fbacktrace
else
FFLAGS   = -O
endif
CPP      = /lib/cpp -traditional

ifeq ($(shell `pkg-config --exists ggd`; echo $$?),0)
GGD_VERSION ?= `pkg-config ggd --modversion`
else
GGD_VERSION ?= '0.0.0'
endif

LDEXTRA += -lstdc++

#!!!Local Variables:
#!!! mode: Makefile
#!!! End:

