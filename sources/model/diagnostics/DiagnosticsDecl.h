#include "Chords.h"
class Chord;
class ChordProperties;
DG_DEFINE_PTR(Chord)
DG_DEFINE_LIST(Chord)

#include "Sources.h"
class Source;
DG_DEFINE_PTR(Source)
DG_DEFINE_LIST(Source)

#include "Comment.h"
class Comment;
DG_DEFINE_PTR(Comment)
DG_DEFINE_LIST(Comment)

