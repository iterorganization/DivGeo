#include "Equil.h"
class Equil;
DG_DEFINE_PTR(Equil)

#include "XPointSeg.h"
class XPointSeg;
DG_DEFINE_PTR(XPointSeg)
DG_DEFINE_LIST(XPointSeg)

#include "XPointTest.h"
class XPointTest;
DG_DEFINE_PTR(XPointTest)
DG_DEFINE_LIST(XPointTest)

#include "GridPointEx.h"
class GridPointEx;
DG_DEFINE_PTR(GridPointEx)
DG_DEFINE_LIST(GridPointEx)

#include "GridPointSeg.h"
class GridPointSeg;
DG_DEFINE_PTR(GridPointSeg)
DG_DEFINE_LIST(GridPointSeg)

#include "SurfaceEx.h"
class SurfaceEx;
DG_DEFINE_PTR(SurfaceEx)
DG_DEFINE_LIST(SurfaceEx)
struct SurfaceExParams;

#include "SurfaceZone.h"
class SurfaceZone;
DG_DEFINE_PTR(SurfaceZone)
DG_DEFINE_LIST(SurfaceZone)
